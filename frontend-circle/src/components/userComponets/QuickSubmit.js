export default function QuickJobSubmit({ onJobSubmit }) {
    // Form state and submission logic here
  
    return (
      <div>
        <h2>Submit a New Job</h2>
        <form onSubmit={/* Form submission logic */}>
          {/* Form fields */}
          <button type="submit">Submit Job</button>
        </form>
      </div>
    );
  }
  